
<%= render "shared/action_bar" %>

<div class="menu">
  <select>
    <option>today</option>
    <option>tai</option>
    <option>mom</option>
  </select>
  <%= render :partial => "entry", :collection => current_user.entries %>
</div>

<div class="recipes">
  <div class="recipe-type"> 
    <a href="?tab=news" title="Recipes from your friends and people you followed">News</a>|
    <a href="?tab=interesting" title="Recipes that you interested with">Interesting</a>|
    Hot | Week | Summer </div>
  <%= render :partial => "feed", :collection => @recipes %>

  <%= paginate @recipes %>
</div>
